---
# defaults file for ansible-role-cmd

# cmd-os or cmd-one
cmd_distribution: cmd-os

# cmd release number
cmd_release: 1

# OpenStack version to use
cmd_openstack_release: pike

# whether to enable or not the candidate repo
cmd_enable_candidate_repo: false
